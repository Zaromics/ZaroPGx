# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts always use LF line endings (required for WSL execution)
*.sh text eol=lf
*.bash text eol=lf

# Ensure Windows batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
